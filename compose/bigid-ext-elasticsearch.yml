version: '3.2'
services:
  bigid-metadata-search:
    environment:
      - BIGID_ELASTICSEARCH_EXTERNAL_FULL_URL=${BIGID_ELASTICSEARCH_EXTERNAL_FULL_URL}
      - BIGID_ELASTICSEARCH_USER=${BIGID_ELASTICSEARCH_USER}
      - BIGID_ELASTICSEARCH_PWD=${BIGID_ELASTICSEARCH_PWD}
      - BIGID_TENANT_ID=${BIGID_TENANT_ID}
      - BIGID_ELASTICSEARCH_USE_SSL
      - BIGID_ELASTICSEARCH_SSL_CERT_PATH
      - BIGID_APP_ACCESS_TOKEN=${BIGID_APP_ACCESS_TOKEN}
