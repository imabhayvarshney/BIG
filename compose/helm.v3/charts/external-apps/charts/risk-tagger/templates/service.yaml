{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "risk-tagger.fullname" . }}
  labels:
    {{- include "risk-tagger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "risk-tagger.selectorLabels" . | nindent 4 }}
{{- end }}
