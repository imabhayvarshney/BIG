{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "alation.fullname" . }}
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    {{- include "alation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alation.selectorLabels" . | nindent 4 }}
{{- end }}
