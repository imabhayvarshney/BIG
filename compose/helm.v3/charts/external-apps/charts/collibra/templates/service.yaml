{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "collibra.fullname" . }}
  labels:
    {{- include "collibra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "collibra.selectorLabels" . | nindent 4 }}
{{- end }}
