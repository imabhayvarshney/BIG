{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "gcp-discovery.fullname" . }}
  labels:
    {{- include "gcp-discovery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gcp-discovery.selectorLabels" . | nindent 4 }}
{{- end }}
