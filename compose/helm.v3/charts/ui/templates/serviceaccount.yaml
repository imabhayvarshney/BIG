{{- if .Values.global.bigid.ui.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-ui
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-ui
  {{- with .Values.global.bigid.ui.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
