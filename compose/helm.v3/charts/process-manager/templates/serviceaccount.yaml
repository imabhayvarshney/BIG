{{- if .Values.global.bigid.processManager.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-process-manager
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-process-manager
  {{- with .Values.global.bigid.processManager.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
