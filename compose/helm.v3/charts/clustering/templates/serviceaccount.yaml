{{- if .Values.global.bigid.clustering.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-clustering
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-clustering
  {{- with .Values.global.bigid.clustering.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
