{{- if .Values.global.bigid.aci.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-aci
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-aci
  {{- with .Values.global.bigid.aci.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
