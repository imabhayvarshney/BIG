{{- if .Values.global.bigid.orchestrator.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-orch
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-orch
  {{- with .Values.global.bigid.orchestrator.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
