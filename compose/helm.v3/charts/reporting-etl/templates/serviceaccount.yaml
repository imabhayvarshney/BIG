{{- if .Values.global.bigid.reportingEtl.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-reporting-etl
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-reporting-etl
  {{- with .Values.global.bigid.reportingEtl.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
