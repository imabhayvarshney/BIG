{{- if .Values.global.bigid.reports.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-reports
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-reports
  {{- with .Values.global.bigid.reports.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
