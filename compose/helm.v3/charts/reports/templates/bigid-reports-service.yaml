apiVersion: v1
kind: Service
metadata:
  name: bigid-reports
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-reports
    {{- with .Values.global.additionalLabels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ports:
    - port: {{ .Values.global.bigid.reports.port }}
      targetPort: {{ .Values.global.bigid.reports.port }}
      name: http
  selector:
    app: bigid-reports
