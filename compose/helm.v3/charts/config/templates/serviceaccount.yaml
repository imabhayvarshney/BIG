{{- if .Values.global.bigid.configService.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-config-service
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-config-service
  {{- with .Values.global.bigid.configService.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
