{{- if .Values.global.bigid.orchestrator2.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-orch2
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-orch2
  {{- with .Values.global.bigid.orchestrator2.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
