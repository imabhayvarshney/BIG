apiVersion: v1
kind: Service
metadata:
  name: bigid-orch2
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-orch2
    {{- with .Values.global.additionalLabels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ports:
   - port: {{ .Values.global.bigid.orchestrator2.port }}
     targetPort: {{ .Values.global.bigid.orchestrator2.port }}
     name: http
  selector:
    app: bigid-orch2
