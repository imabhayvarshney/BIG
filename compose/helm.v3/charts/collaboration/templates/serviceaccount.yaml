{{- if .Values.global.bigid.collaboration.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-collaboration
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-collaboration
  {{- with .Values.global.bigid.collaboration.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
