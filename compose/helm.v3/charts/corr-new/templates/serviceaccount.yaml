{{- if .Values.global.bigid.correlator.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-corr-new
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-corr-new
  {{- with .Values.global.bigid.correlator.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
