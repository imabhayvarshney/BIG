apiVersion: v1
kind: Service
metadata:
  name: bigid-corr-new
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-corr-new
    {{- with .Values.global.additionalLabels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ports:
   - port: {{ .Values.global.bigid.correlator.port }}
     targetPort: {{ .Values.global.bigid.correlator.port }}
     name: http
  selector:
    app: bigid-corr-new
