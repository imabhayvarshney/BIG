{{- if .Values.global.bigid.web.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bigid-web
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: bigid-web
  {{- with .Values.global.bigid.web.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
